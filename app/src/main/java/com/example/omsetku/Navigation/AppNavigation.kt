import com.example.omsetku.ui.screen.TransactionDetailScreen

        // Detail Transaksi
        composable(
            route = Routes.TRANSACTION_DETAIL
        ) {
            TransactionDetailScreen(navController = navController)
        } 